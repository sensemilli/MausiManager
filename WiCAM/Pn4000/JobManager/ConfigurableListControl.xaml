<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="WiCAM.Pn4000.JobManager.ConfigurableListControl" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:local="clr-namespace:WiCAM.Pn4000.JobManager">
  <Grid Background="#FFF5F5F5">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition />
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition />
      <ColumnDefinition Width="5" />
      <ColumnDefinition Width="25" />
      <ColumnDefinition Width="5" />
    </Grid.ColumnDefinitions>
    <TextBox Grid.Row="0" Grid.Column="0" Margin="3" Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}" />
    <ListBox Grid.Row="1" Grid.Column="0" Margin="3" BorderThickness="0" ItemsSource="{Binding Texts}" SelectedItem="{Binding SelectedText}" />
    <StackPanel Grid.Row="1" Grid.Column="2">
      <Button Content="+" Height="25" Margin="0,3,0,5" Command="{Binding AddTextCommand}" />
      <Button Content="-" Height="25" Command="{Binding DeleteTextCommand}" />
    </StackPanel>
  </Grid>
</UserControl>