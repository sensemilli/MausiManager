<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="WiCAM.Pn4000.GuiWpf.TabUnfold.ValidationView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:local="clr-namespace:WiCAM.Pn4000.GuiWpf.TabUnfold" xmlns:vm="clr-namespace:WiCAM.Pn4000.GuiWpf.TabUnfold" xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="3*" />
      <RowDefinition Height="5" />
      <RowDefinition Height="1*" />
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="Auto" />
      <ColumnDefinition Width="5" />
      <ColumnDefinition Width="5*" />
    </Grid.ColumnDefinitions>
    <Button Grid.Row="0" Grid.Column="0" BorderThickness="0" Background="#9fff" HorizontalContentAlignment="Left" Visibility="{Binding VisibilityValidationResults}" Command="{Binding CmdExpandValiResults}">
      <StackPanel Orientation="Horizontal">
        <controls:RadGlyph xmlns:controls="clr-namespace:Telerik.Windows.Controls;assembly=Telerik.Windows.Controls" controls:RadGlyph.Glyph="" controls:RadGlyph.Foreground="#FF008000" controls:RadGlyph.FontSize="30" UIElement.Visibility="{Binding VisibilityValiOk}" />
                <telerik:RadGlyph telerik:RadGlyph.Glyph="" telerik:RadGlyph.Foreground="#FFFF8C00" telerik:RadGlyph.FontSize="30" UIElement.Visibility="{Binding VisibilityValiWarning}" />
        <TextBlock Padding="5,0,5,0" VerticalAlignment="Center" Text="{Binding ValidationErrorCount}" Visibility="{Binding VisibilityValiWarning}" />
      </StackPanel>
    </Button>
    <Grid Row="1" Column="0" Visibility="{Binding VisibilityValiResultDetails}">
      <Grid.RowDefinitions>
        <RowDefinition Height="1*" />
        <RowDefinition Height="0*" />
      </Grid.RowDefinitions>
      <TextBlock MaxHeight="20" VerticalAlignment="Top" Background="#9fff" Text="{DynamicResource l_popup.PopupUnfoldInfo.ValidationOK}" Visibility="{Binding VisibilityValiOk}" />
      <ScrollViewer Background="#9fff" VerticalScrollBarVisibility="Auto" Visibility="{Binding VisibilityValiWarning}">
        <Grid>
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="5" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
          </Grid.RowDefinitions>
          <FrameworkElement.Resources>
            <ResourceDictionary>
              <Style x:Key="{x:Type DataGridRow}" TargetType="{x:Type DataGridRow}">
                <Setter Property="DataGridRow.IsSelected" Value="{Binding IsSelected}" />
              </Style>
              <Style x:Key="{x:Type DataGridCell}" TargetType="{x:Type DataGridCell}">
                <Style.Triggers>
                  <Trigger Property="DataGridCell.IsSelected" Value="True">
                    <Setter Property="Background" Value="#FF87CEFA" />
                    <Setter Property="Foreground" Value="#FF000000" />
                  </Trigger>
                </Style.Triggers>
              </Style>
            </ResourceDictionary>
          </FrameworkElement.Resources>
          <GridSplitter Grid.Row="3" HorizontalAlignment="Stretch" />
          <Label Grid.Row="0" FontSize="14" Content="{DynamicResource UnfoldView.ValiSelfCollisionDetected}" Visibility="{Binding SelfCollisionDetected}" />
          <Label Grid.Row="1" FontSize="16" FontWeight="Bold" Content="{DynamicResource UnfoldView.ValiIntrinsicWarnings}" />
          <DataGrid Grid.Row="2" AutoGenerateColumns="False" CanUserDeleteRows="False" CanUserAddRows="False" IsReadOnly="True" ItemsSource="{Binding ValiIntrinsicErrors}" SelectionChanged="Selector_OnSelectionChanged">
            <DataGrid.Columns>
              <DataGridTextColumn Header="{DynamicResource UnfoldView.ValiType}" Binding="{Binding Type}" />
              <DataGridTextColumn Header="{DynamicResource UnfoldView.ValiErrormessage}" Binding="{Binding ErrorDesc}" />
            </DataGrid.Columns>
          </DataGrid>
          <Label Grid.Row="4" FontSize="16" FontWeight="Bold" Content="{DynamicResource UnfoldView.ValiDistanceWarnings}" />
          <DataGrid Grid.Row="5" AutoGenerateColumns="False" CanUserDeleteRows="False" CanUserAddRows="False" IsReadOnly="True" ItemsSource="{Binding ValiDistancesErrors}" SelectionChanged="Selector_OnSelectionChanged">
            <DataGrid.Columns>
              <DataGridTextColumn Header="{DynamicResource UnfoldView.ValiType}" Binding="{Binding Type1}" />
              <DataGridTextColumn Header="{DynamicResource UnfoldView.ValiType}" Binding="{Binding Type2}" />
            </DataGrid.Columns>
          </DataGrid>
        </Grid>
      </ScrollViewer>
    </Grid>
    <GridSplitter Grid.Row="2" Grid.Column="0" Background="#9fff" HorizontalAlignment="Stretch" Visibility="{Binding VisibilityValiResultDetails}" />
  </Grid>
</UserControl>