<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="WiCAM.Pn4000.GuiWpf.GeneralSubWindow.IssueView" 
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WiCAM.Pn4000.GuiWpf.GeneralSubWindow" 
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation" 
             xmlns:controls="clr-namespace:WiCAM.Pn4000.WpfBasics.Controls;assembly=WiCAM.Pn4000.WpfBasics">
  <FrameworkElement.Resources>
    <ResourceDictionary>
      <Style x:Key="NotificationOnButton" TargetType="{x:Type Button}">
        <Setter Property="FrameworkElement.Cursor" Value="Hand" />
        <Setter Property="Panel.ZIndex" Value="50000" />
        <Style.Triggers>
          <EventTrigger RoutedEvent="Button.Loaded">
            <EventTrigger.Actions>
              <BeginStoryboard>
                <Storyboard BeginTime="00:00:00">
                  <ColorAnimation Storyboard.TargetProperty="(Button.Background).(SolidColorBrush.Color)" From="Transparent" To="OrangeRed" Duration="0:0:0.5" AutoReverse="True" RepeatBehavior="Forever" />
                </Storyboard>
              </BeginStoryboard>
            </EventTrigger.Actions>
          </EventTrigger>
        </Style.Triggers>
      </Style>
    </ResourceDictionary>
  </FrameworkElement.Resources>
  <Grid>
    <WrapPanel>
      <Button ToolTip="{DynamicResource l_popup.PopupUnfoldInfo.NoMaterialByUser}" Command="{Binding CmdWarningMaterial}" Visibility="{Binding WarningMaterialVisibility}">
        <controls1:RadGlyph xmlns:controls1="clr-namespace:Telerik.Windows.Controls;assembly=Telerik.Windows.Controls" controls1:RadGlyph.Glyph="" controls1:RadGlyph.Foreground="#FFFF8C00" controls1:RadGlyph.FontSize="30" />
      </Button>
      <Button ToolTip="{DynamicResource l_popup.PopupUnfoldInfo.KFactorWarning}" Command="{Binding CmdWarningKFactor}" Visibility="{Binding WarningKFactorVisibility}">
        <RadGlyph p11:RadGlyph.Glyph="" p11:RadGlyph.Foreground="#FFFF8C00" p11:RadGlyph.FontSize="30" xmlns:p11="clr-namespace:Telerik.Windows.Controls;assembly=Telerik.Windows.Controls" xmlns="clr-namespace:Telerik.Windows.Controls;assembly=Telerik.Windows.Controls" />
      </Button>
      <Button ToolTip="{DynamicResource l_popup.PopupUnfoldInfo.ToolShortageWarning}" Command="{Binding CmdWarningToolShortage}" Visibility="{Binding WarningToolShortageVisibility}">
        <RadGlyph p11:RadGlyph.Glyph="" p11:RadGlyph.Foreground="#FFFF8C00" p11:RadGlyph.FontSize="30" xmlns:p11="clr-namespace:Telerik.Windows.Controls;assembly=Telerik.Windows.Controls" xmlns="clr-namespace:Telerik.Windows.Controls;assembly=Telerik.Windows.Controls" />
      </Button>
      <controls:WiButton FrameworkElement.Style="{StaticResource NotificationOnButton}" Control.FontSize="20" RadGlyph="" ButtonBase.Command="{Binding CmdWarningUpdateDoc}" UIElement.Visibility="{Binding WarningUpdateDocVisibility}" FrameworkElement.ToolTip="{Binding WarningUpdateDocTooltip}">
        <UIElement.RenderTransform>
          <RotateTransform />
        </UIElement.RenderTransform>
      </controls:WiButton>
      <controls:WiButton Control.FontSize="15" RadGlyph="" ButtonBase.Command="{Binding CmdWarningUpdateDoc}" UIElement.Visibility="{Binding DefaultUpdateDocVisibility}" FrameworkElement.ToolTip="{Binding WarningUpdateDocTooltip}" />
    </WrapPanel>
  </Grid>
</UserControl>