<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="WiCAM.Pn4000.GuiWpf.Ui3D.MachineConfig.PPSettingsView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:viewModels="clr-namespace:WiCAM.Pn4000.PN3D.Popup.UI.ViewModels" Background="#00FFFFFF" VerticalContentAlignment="Stretch" HorizontalContentAlignment="Stretch" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" xmlns:machineconfig="clr-namespace:WiCAM.Pn4000.GuiWpf.Ui3D.MachineConfig">
  <Border BorderBrush="#FFA9A9A9" BorderThickness="1" CornerRadius="7.5">
    <Border.Background>
      <SolidColorBrush Color="WhiteSmoke" Opacity="0.8" />
    </Border.Background>
    <StackPanel HorizontalAlignment="Center" Orientation="Vertical" Grid.IsSharedSizeScope="True">
      <TextBlock Text="{DynamicResource l_popup.PpView.OutputSettings}" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="20" FontWeight="Bold" TextDecorations="Underline" Grid.ColumnSpan="2" Margin="0" />
      <Grid Margin="2">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" SharedSizeGroup="gr0" />
          <ColumnDefinition Width="Auto" SharedSizeGroup="gr1" />
          <ColumnDefinition Width="Auto" SharedSizeGroup="gr2" />
        </Grid.ColumnDefinitions>
        <Label Content="{DynamicResource l_popup.PpView.OutputPath}" />
        <TextBox Grid.Column="1" MinWidth="200" Text="{Binding NCDirectory}" />
        <Button Grid.Column="2" Content="..." MinWidth="30" Command="{Binding SelectOutputPathCommand}" />
      </Grid>
      <Grid Margin="2">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" SharedSizeGroup="gr0" />
          <ColumnDefinition Width="Auto" SharedSizeGroup="gr1" />
        </Grid.ColumnDefinitions>
        <Label Content="{DynamicResource l_popup.PpView.OutputPathUserSelect}" />
        <CheckBox Grid.Column="1" VerticalContentAlignment="Center" IsChecked="{Binding NCDirectoryUserSelect}" />
      </Grid>
      <Grid Margin="2">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" SharedSizeGroup="gr0" />
          <ColumnDefinition Width="Auto" SharedSizeGroup="gr1" />
          <ColumnDefinition Width="Auto" SharedSizeGroup="gr2" />
        </Grid.ColumnDefinitions>
        <Label Content="{DynamicResource l_popup.PpView.OutputPathReport}" />
        <TextBox Grid.Column="1" Text="{Binding ReportDirectory}" />
        <Button Grid.Column="2" Content="..." MinWidth="30" Command="{Binding SelectReportOutputPathCommand}" />
      </Grid>
      <Grid Margin="2">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" SharedSizeGroup="gr0" />
          <ColumnDefinition Width="Auto" SharedSizeGroup="gr1" />
        </Grid.ColumnDefinitions>
        <Label Content="{DynamicResource l_popup.PpView.DllPath}" />
        <TextBox Grid.Column="1" Text="{Binding PP}">
          <UIElement.InputBindings>
            <MouseBinding MouseAction="LeftClick" Command="{Binding SelectDllPathCommand}" />
          </UIElement.InputBindings>
        </TextBox>
      </Grid>
      <Grid Margin="2">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" SharedSizeGroup="gr0" />
          <ColumnDefinition Width="Auto" SharedSizeGroup="gr1" />
        </Grid.ColumnDefinitions>
        <Label Content="{DynamicResource l_popup.PpView.PpExtension}" />
        <TextBox Grid.Column="1" Text="{Binding Extension, UpdateSourceTrigger=PropertyChanged}" />
      </Grid>
      <Grid Margin="2">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" SharedSizeGroup="gr0" />
          <ColumnDefinition Width="Auto" SharedSizeGroup="gr1" />
        </Grid.ColumnDefinitions>
        <Label Content="{DynamicResource l_popup.PpView.AddDateTime}" />
        <CheckBox Grid.Column="1" VerticalContentAlignment="Center" IsChecked="{Binding AddDateTime}" />
      </Grid>
      <Grid Margin="2">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" SharedSizeGroup="gr0" />
          <ColumnDefinition Width="Auto" SharedSizeGroup="gr1" />
        </Grid.ColumnDefinitions>
        <Label Content="{DynamicResource l_popup.PpView.AddGeometryToNcFile}" />
        <CheckBox Grid.Column="1" VerticalContentAlignment="Center" IsChecked="{Binding AddGeometryToNcFile}" />
      </Grid>
      <Grid Margin="2">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" SharedSizeGroup="gr0" />
          <ColumnDefinition Width="Auto" SharedSizeGroup="gr1" />
        </Grid.ColumnDefinitions>
        <Label Content="{DynamicResource l_popup.PpView.OpenNcFileAfterCreation}" />
        <CheckBox Grid.Column="1" VerticalContentAlignment="Center" IsChecked="{Binding OpenNcFileAfterCreation}" />
      </Grid>
      <Grid Margin="2">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" SharedSizeGroup="gr0" />
          <ColumnDefinition Width="Auto" SharedSizeGroup="gr1" />
        </Grid.ColumnDefinitions>
        <Label Content="{DynamicResource l_popup.PpView.NcNameAuto}" ToolTip="{DynamicResource l_popup.PpView.NcNameAutoTt}" />
        <CheckBox Grid.Column="1" VerticalContentAlignment="Center" ToolTip="{DynamicResource l_popup.PpView.NcNameAutoTt}" IsChecked="{Binding PpNameAuto}" />
      </Grid>
      <Grid Margin="2">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" SharedSizeGroup="gr0" />
          <ColumnDefinition Width="Auto" SharedSizeGroup="gr1" />
        </Grid.ColumnDefinitions>
        <Label Content="{DynamicResource l_popup.PpView.NcNameFormat}">
          <FrameworkElement.ToolTip>
            <TextBlock>
              <Run Text="{DynamicResource l_popup.PpView.NcNameFormatTt}" />
              <LineBreak />
              <Run Text="{Binding NcNameFormatExample, Mode=OneWay}" />
            </TextBlock>
          </FrameworkElement.ToolTip>
        </Label>
        <TextBox Grid.Column="1" VerticalContentAlignment="Center" Text="{Binding NcNameFormat, UpdateSourceTrigger=PropertyChanged}">
          <FrameworkElement.ToolTip>
            <TextBlock>
              <Run Text="{DynamicResource l_popup.PpView.NcNameFormatTt}" />
              <LineBreak />
              <Run Text="{Binding NcNameFormatExample, Mode=OneWay}" />
            </TextBlock>
          </FrameworkElement.ToolTip>
        </TextBox>
      </Grid>
      <Grid Margin="2">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" SharedSizeGroup="gr0" />
          <ColumnDefinition Width="Auto" SharedSizeGroup="gr1" />
        </Grid.ColumnDefinitions>
        <Label Content="{DynamicResource l_popup.PpView.NcNameFormatMulti}">
          <FrameworkElement.ToolTip>
            <TextBlock>
              <Run Text="{DynamicResource l_popup.PpView.NcNameFormatMultiTt}" />
              <LineBreak />
              <Run Text="{Binding NcNameFormatExampleMulti, Mode=OneWay}" />
            </TextBlock>
          </FrameworkElement.ToolTip>
        </Label>
        <TextBox Grid.Column="1" VerticalContentAlignment="Center" Text="{Binding NcNameFormatMulti, UpdateSourceTrigger=PropertyChanged}">
          <FrameworkElement.ToolTip>
            <TextBlock>
              <Run Text="{DynamicResource l_popup.PpView.NcNameFormatMultiTt}" />
              <LineBreak />
              <Run Text="{Binding NcNameFormatExampleMulti, Mode=OneWay}" />
            </TextBlock>
          </FrameworkElement.ToolTip>
        </TextBox>
      </Grid>
      <Grid Margin="2">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" SharedSizeGroup="gr0" />
          <ColumnDefinition Width="Auto" SharedSizeGroup="gr1" />
        </Grid.ColumnDefinitions>
        <Label Content="{DynamicResource l_popup.PpView.NcNameNextNo}" />
        <TextBox Grid.Column="1" VerticalContentAlignment="Center" Text="{Binding NcNameNextNo}" />
      </Grid>
      <Grid Margin="2">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" SharedSizeGroup="gr0" />
          <ColumnDefinition Width="Auto" SharedSizeGroup="gr1" />
        </Grid.ColumnDefinitions>
        <Label Content="{DynamicResource l_popup.PpView.NcMachineName}" />
        <TextBox Grid.Column="1" VerticalContentAlignment="Center" Text="{Binding NcMachineName}" />
      </Grid>
      <Grid Margin="2">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" SharedSizeGroup="gr0" />
          <ColumnDefinition Width="Auto" SharedSizeGroup="gr1" />
        </Grid.ColumnDefinitions>
        <Label Content="{DynamicResource l_popup.PpView.NcMachineName2}" />
        <TextBox Grid.Column="1" VerticalContentAlignment="Center" Text="{Binding NcMachineName2}" />
      </Grid>
      <Grid Margin="2">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" SharedSizeGroup="gr0" />
          <ColumnDefinition Width="Auto" SharedSizeGroup="gr1" />
        </Grid.ColumnDefinitions>
        <Label Content="{DynamicResource l_popup.PpView.NcMachineType}" />
        <TextBox Grid.Column="1" VerticalContentAlignment="Center" Text="{Binding NcMachineType}" />
      </Grid>
      <Grid Margin="2">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" SharedSizeGroup="gr0" />
          <ColumnDefinition Width="Auto" SharedSizeGroup="gr1" />
        </Grid.ColumnDefinitions>
        <Label Content="{DynamicResource l_popup.PpView.NcMachineType2}" />
        <TextBox Grid.Column="1" VerticalContentAlignment="Center" Text="{Binding NcMachineType2}" />
      </Grid>
    </StackPanel>
  </Border>
</UserControl>