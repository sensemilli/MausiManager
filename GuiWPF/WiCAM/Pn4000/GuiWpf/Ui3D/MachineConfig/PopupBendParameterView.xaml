<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="WiCAM.Pn4000.GuiWpf.Ui3D.MachineConfig.PopupBendParameterView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:views="clr-namespace:WiCAM.Pn4000.PN3D.Popup.UI.Views" xmlns:viewModel="clr-namespace:WiCAM.Pn4000.GuiWpf.Ui3D.MachineConfig" xmlns:views1="clr-namespace:WiCAM.Pn4000.PN3D.Popup.UI.Views;assembly=WiCAM.Pn4000.PN3D" xmlns:tools="clr-namespace:WiCAM.Pn4000.GuiWpf.Ui3D.MachineConfig.Tools" xmlns:subBendTable="clr-namespace:WiCAM.Pn4000.GuiWpf.Ui3D.MachineConfig.SubBendTable" VerticalContentAlignment="Stretch" HorizontalContentAlignment="Stretch" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Background="#00FFFFFF">
  <FrameworkElement.Resources>
    <ResourceDictionary>
      <Style x:Key="TabItemStyle" TargetType="{x:Type TabItem}">
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="{x:Type TabItem}">
              <Border Name="Panel" BorderBrush="#FFA9A9A9" BorderThickness="1" CornerRadius="3" Height="35">
                <Border.Background>
                  <SolidColorBrush Color="WhiteSmoke" Opacity="0.7" />
                </Border.Background>
                <Grid Background="#00FFFFFF">
                  <ContentPresenter Name="ContentSite" VerticalAlignment="Center" HorizontalAlignment="Center" ContentSource="Header" Margin="10,2" />
                </Grid>
              </Border>
              <ControlTemplate.Triggers>
                <Trigger Property="TabItem.IsSelected" Value="True">
                  <Setter TargetName="Panel" Property="Border.Background" Value="#FFD3D3D3" />
                  <Setter TargetName="Panel" Property="UIElement.Opacity" Value="0.7" />
                </Trigger>
                <Trigger Property="TabItem.IsSelected" Value="False">
                  <Setter TargetName="Panel" Property="Border.Background" Value="#FFF5F5F5" />
                  <Setter TargetName="Panel" Property="UIElement.Opacity" Value="0.7" />
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>
    </ResourceDictionary>
  </FrameworkElement.Resources>
  <Border BorderBrush="#FFA9A9A9" BorderThickness="1" CornerRadius="7.5" Margin="5">
    <Border.Background>
      <SolidColorBrush Color="WhiteSmoke" Opacity="{Binding DialogOpacity}" />
    </Border.Background>
    <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="1*" />
        <ColumnDefinition Width="0.1*" />
      </Grid.ColumnDefinitions>
      <Grid.RowDefinitions>
        <RowDefinition Height="1*" />
      </Grid.RowDefinitions>
      <DockPanel Grid.Row="0" Grid.Column="1" Margin="2,0,2,0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <StackPanel Margin="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" DockPanel.Dock="Top">
          <Button Margin="5,1,5,3" Style="{DynamicResource PnPopupOptionButton}" Height="25" Content="{DynamicResource l_popup.PopupBendParameter.Delete}" Command="{Binding Button0_DeleteClick}" IsEnabled="{Binding IsDeleteButtonEnabled}" />
          <Button Margin="5,1,5,3" Style="{DynamicResource PnPopupOptionButton}" Height="25" Content="{DynamicResource l_popup.PopupBendParameter.Add}" Command="{Binding Button2_AddClick}" IsEnabled="{Binding IsAddNewButtonEnabled}" />
          <Button Margin="5,1,5,3" Style="{DynamicResource PnPopupOptionButton}" Height="25" Content="{DynamicResource l_popup.PopupBendParameter.Copy}" Command="{Binding Button8_CopyClick}" IsEnabled="{Binding IsCopyButtonEnabled}" />
          <Button Margin="5,1,5,3" Style="{DynamicResource PnPopupOptionButton}" Height="25" Content="{DynamicResource l_popup.PopupBendParameter.Edit}" Command="{Binding Button1_EditClick}" IsEnabled="{Binding IsEditButtonEnabled}" />
        </StackPanel>
        <StackPanel VerticalAlignment="Bottom" Margin="0" HorizontalAlignment="Stretch">
          <Button Margin="5,1,5,3" Style="{DynamicResource PnPopupOptionButton}" Height="25" Content="{DynamicResource l_popup.PopupBendParameter.Export}" Command="{Binding Button10_GraphicsClick}" IsEnabled="{Binding IsAddNewButtonEnabled}" />
          <Button Margin="5,1,5,3" Style="{DynamicResource PnPopupOptionButton}" Height="25" Content="{DynamicResource l_popup.PopupBendParameter.Import}" Command="{Binding Button15_PrintClick}" IsEnabled="{Binding IsAddNewButtonEnabled}" />
          <Button Margin="5,1,5,3" Style="{DynamicResource PnPopupOptionButton}" Height="25" Content="{DynamicResource l_popup.PopupBendParameter.Save}" Command="{Binding Button7_LearnClick}" IsEnabled="{Binding IsSaveButtonEnabled}" />
          <Button Margin="5,1,5,3" Style="{DynamicResource PnPopupOptionButton}" Height="25" Content="{DynamicResource l_popup.PopupBendParameter.Cancel}" Command="{Binding Button5_CancelClick}" IsEnabled="{Binding IsCancelButtonEnabled}" />
          <Button Margin="5,1,5,3" Style="{DynamicResource PnPopupOptionButton}" Height="25" Content="{DynamicResource l_popup.PopupBendParameter.Ok}" Command="{Binding Button16_OkClick}" IsEnabled="{Binding IsOkButtonEnabled}" />
        </StackPanel>
      </DockPanel>
    </Grid>
  </Border>
</UserControl>