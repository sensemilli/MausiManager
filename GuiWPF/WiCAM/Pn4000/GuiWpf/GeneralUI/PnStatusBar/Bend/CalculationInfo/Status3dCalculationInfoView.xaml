<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="WiCAM.Pn4000.GuiWpf.GeneralUI.PnStatusBar.Bend.CalculationInfo.Status3dCalculationInfoView" 
             x:ClassModifier="internal" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WiCAM.Pn4000.GuiWpf.GeneralUI.PnStatusBar.Bend.CalculationInfo" 
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation">
  <Grid Cursor="">
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="Auto" />
      <ColumnDefinition Width="Auto" />
      <ColumnDefinition Width="Auto" />
      <ColumnDefinition Width="1*" />
    </Grid.ColumnDefinitions>
    <Button Grid.Column="0" Cursor="Arrow" Command="{Binding CmdCancel}" Visibility="{Binding CancelVisibility}" Content="{Binding CancelDesc}" />
    <TextBlock Grid.Column="1" Text="{DynamicResource PnInfoFields3D.Calculation.Calculating}" VerticalAlignment="Center" Visibility="{Binding CalculatingPrefixVisibility}" />
    <TextBlock Grid.Column="2" Margin="10,0" VerticalAlignment="Center" Text="{Binding Status}" />
    <Border Grid.Column="3" BorderBrush="#106ebe" BorderThickness="3" Visibility="{Binding ProgressVisibility}">
      <Border BorderBrush="#FFF5F5F5" BorderThickness="3">
        <telerik:RadProgressBar RangeBase.Minimum="0" RangeBase.Maximum="1" Control.BorderBrush="#106ebe" Control.BorderThickness="1" Control.Foreground="#106ebe" telerik:RadProgressBar.IsIndeterminate="{Binding ProgressIndeterminate}" RangeBase.Value="{Binding Progress}" />
      </Border>
    </Border>
  </Grid>
</UserControl>