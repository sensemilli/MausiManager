<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="WiCAM.Pn4000.PN3D.Unfold.UI.View.ContextMenuItemBendView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:local="clr-namespace:WiCAM.Pn4000.PN3D.Unfold.UI.View" xmlns:viewModel="clr-namespace:WiCAM.Pn4000.PN3D.Unfold.UI.ViewModel" xmlns:popup="clr-namespace:WiCAM.Pn4000.Popup;assembly=WiCAM.Pn4000.Popup" Width="AUTO" Margin="0,1,0,0">
  <Border BorderBrush="#FFA9A9A9" BorderThickness="1" CornerRadius="5">
    <Border.Background>
      <SolidColorBrush Color="White" Opacity="0.7" />
    </Border.Background>
    <Grid Margin="1">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="1*" />
        <ColumnDefinition Width="3*" />
        <ColumnDefinition Width="AUTO" />
        <ColumnDefinition Width="AUTO" />
        <ColumnDefinition Width="AUTO" />
      </Grid.ColumnDefinitions>
      <Viewbox Margin="2">
        <Image Source="{Binding Image}" />
      </Viewbox>
      <StackPanel Grid.Column="1" Orientation="Horizontal">
        <TextBlock VerticalAlignment="Center" Text="{Binding Description1}" />
        <TextBox VerticalContentAlignment="Center" Style="{DynamicResource PnPopupTextBoxStyle}" Text="{Binding Value}" IsEnabled="{Binding CanChangeAngle}" />
        <TextBlock VerticalAlignment="Center" Text="{Binding Description2}" />
      </StackPanel>
      <Button Grid.Column="2" Content="OK" Style="{DynamicResource PnPopupOptionButton}" Margin="2" Command="{Binding Command}" IsEnabled="{Binding CanChangeAngle}" />
      <Button Grid.Column="3" Content="X" Style="{DynamicResource PnPopupOptionButton}" Margin="2" Command="{Binding Command2}" IsEnabled="{Binding CanMerge}" />
      <Button Grid.Column="4" Content="Split" Style="{DynamicResource PnPopupOptionButton}" Margin="2" Command="{Binding Command3}" IsEnabled="{Binding CanSplit}" />
    </Grid>
  </Border>
</UserControl>