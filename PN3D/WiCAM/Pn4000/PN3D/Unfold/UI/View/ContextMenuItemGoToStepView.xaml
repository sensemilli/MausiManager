<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="WiCAM.Pn4000.PN3D.Unfold.UI.View.ContextMenuItemGoToStepView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:local="clr-namespace:WiCAM.Pn4000.PN3D.Unfold.UI.View" xmlns:viewModel="clr-namespace:WiCAM.Pn4000.PN3D.Unfold.UI.ViewModel">
  <Border BorderBrush="#FFA9A9A9" BorderThickness="1" CornerRadius="5">
    <Border.Background>
      <SolidColorBrush Color="White" Opacity="0.7" />
    </Border.Background>
    <Grid Margin="1">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="1*" />
        <ColumnDefinition Width="1*" />
        <ColumnDefinition Width="1*" />
        <ColumnDefinition Width="1*" />
      </Grid.ColumnDefinitions>
      <Button Margin="2" Grid.Column="0" Style="{DynamicResource PnPopupOptionButton}" Command="{Binding CommandUpperDpFlat}">
        <ContentControl.Content>
          <Image Margin="2" Source="{Binding ImageUpperDpFlat}" />
        </ContentControl.Content>
      </Button>
      <Button Margin="2" Grid.Column="1" Style="{DynamicResource PnPopupOptionButton}" Command="{Binding CommandClosed}">
        <ContentControl.Content>
          <Image Margin="2" Source="{Binding ImageClosed}" />
        </ContentControl.Content>
      </Button>
      <Button Margin="2" Grid.Column="2" Style="{DynamicResource PnPopupOptionButton}" Command="{Binding CommandBend}">
        <ContentControl.Content>
          <Image Margin="2" Source="{Binding ImageBend}" />
        </ContentControl.Content>
      </Button>
      <Button Margin="2" Grid.Column="3" Style="{DynamicResource PnPopupOptionButton}" Command="{Binding CommandUpperDpBend}">
        <ContentControl.Content>
          <Image Margin="2" Source="{Binding ImageUpperDpBend}" />
        </ContentControl.Content>
      </Button>
    </Grid>
  </Border>
</UserControl>