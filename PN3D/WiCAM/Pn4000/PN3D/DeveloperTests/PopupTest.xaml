<?xml version="1.0" encoding="utf-8"?>
<src1:PopupBase x:Class="WiCAM.Pn4000.PN3D.DeveloperTests.PopupTest" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:src1="clr-namespace:WiCAM.Pn4000.Popup;assembly=WiCAM.Pn4000.Popup" Template="{DynamicResource PopupControlTemplate}" Style="{DynamicResource PopupStyle}" Title="{DynamicResource l_popup.PopupTest.Title}" Height="352" Width="536" xmlns:developertests="clr-namespace:WiCAM.Pn4000.PN3D.DeveloperTests;assembly=WiCAM.Pn4000.PN3D">
  <Grid>
    <StackPanel Orientation="Vertical" Margin="10">
      <TextBlock Style="{DynamicResource PnPopupHeaderText}" Text="Popup at new style" />
      <TextBlock Style="{DynamicResource PnPopupFreeText}" Text="We strive to get a more refreshed user interface. The latest and most interesting solution seems to be the one from Teams from MS. That is why we try to model on it." />
      <Button Height="30" Style="{DynamicResource PnPopupOptionButton}" Content="Example 1: popup with one page" Command="{Binding Option1Click}" />
      <Button Height="30" Style="{DynamicResource PnPopupOptionButton}" Content="Example 2: popup with multi pages" Command="{Binding Option2Click}" />
      <Button Height="30" Style="{DynamicResource PnPopupOptionButton}" Content="Example 3: customized buttons" Command="{Binding Option3Click}" />
      <Button Height="30" Style="{DynamicResource PnPopupOptionButton}" Content="Example 4: disable button (no click)" Command="{Binding Option4Click}" />
    </StackPanel>
  </Grid>
</src1:PopupBase>