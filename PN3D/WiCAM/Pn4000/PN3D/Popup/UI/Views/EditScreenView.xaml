<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="WiCAM.Pn4000.PN3D.Popup.UI.Views.EditScreenView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation" xmlns:viewModels="clr-namespace:WiCAM.Pn4000.PN3D.Popup.UI.ViewModels" Background="#00FFFFFF" VerticalContentAlignment="Stretch" HorizontalContentAlignment="Stretch" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" xmlns:views="clr-namespace:WiCAM.Pn4000.PN3D.Popup.UI.Views">
  <Grid>
    <Panel.Background>
      <SolidColorBrush Color="WhiteSmoke" />
    </Panel.Background>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="1*" />
      <ColumnDefinition Width="0.5*" />
    </Grid.ColumnDefinitions>
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="1*" />
        <RowDefinition Height="AUTO" />
      </Grid.RowDefinitions>
      <telerik:RadPropertyGrid BorderThickness="1,1,1,0" AutoGeneratePropertyDefinitions="True" Item="{Binding SelectedItem}" AutoGeneratingPropertyDefinition="PropertyGrid_AutoGeneratingPropertyDefinition" />
      <Border Grid.Row="1" Background="#FFFFFFFF" BorderThickness="1" BorderBrush="#FFA9A9A9">
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
          </Grid.ColumnDefinitions>
          <StackPanel Orientation="Horizontal" FlowDirection="LeftToRight" Margin="10,5">
            <Button Width="25" Height="25" Style="{DynamicResource PnPopupOptionButton}" Command="{Binding PreviousItemCommand}">
              <ContentControl.Content>
                <telerik:RadGlyph Glyph="" />
              </ContentControl.Content>
            </Button>
            <TextBlock Margin="10,0" Width="AUTO" VerticalAlignment="Center" HorizontalAlignment="Center" Text="{Binding IndexOfCount}" />
            <Button Width="25" Height="25" Style="{DynamicResource PnPopupOptionButton}" Command="{Binding NextItemCommand}">
              <ContentControl.Content>
                <telerik:RadGlyph Glyph="" />
              </ContentControl.Content>
            </Button>
          </StackPanel>
          <StackPanel Orientation="Horizontal" FlowDirection="RightToLeft" Margin="10,5" Grid.Column="1">
            <Button Content="Cancel" Width="100" Height="25" Style="{DynamicResource PnPopupOptionButton}" Command="{Binding CancelCommand}" />
            <Button Margin="10,0" Content="OK" Width="100" Height="25" Style="{DynamicResource PnPopupOptionButton}" Command="{Binding OkCommand}" />
            <Button Content="Apply" Width="100" Height="25" Style="{DynamicResource PnPopupOptionButton}" Command="{Binding ApplyCommand}" />
          </StackPanel>
        </Grid>
      </Border>
    </Grid>
    <Viewbox Grid.Column="2" Margin="5" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Visibility="{Binding Visible2D}">
      <ContentControl VerticalContentAlignment="Stretch" HorizontalContentAlignment="Stretch" Content="{Binding PreviewImage}" />
    </Viewbox>
    <ContentControl Grid.Column="2" Margin="5" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" VerticalContentAlignment="Stretch" HorizontalContentAlignment="Stretch" Content="{Binding PreviewImage3D}" Visibility="{Binding Visible3D}" />
  </Grid>
</UserControl>