<?xml version="1.0" encoding="utf-8"?>
<src1:PopupBase x:Class="WiCAM.Pn4000.PN3D.Popup.View.PopupSelectPnColorView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:local="clr-namespace:WiCAM.Pn4000.PN3D.Popup.View" xmlns:popup="clr-namespace:WiCAM.Pn4000.Popup;assembly=WiCAM.Pn4000.Popup" xmlns:viewModel="clr-namespace:WiCAM.Pn4000.PN3D.Popup.ViewModel" xmlns:src1="clr-namespace:WiCAM.Pn4000.Popup;assembly=WiCAM.Pn4000.Popup" Template="{DynamicResource PopupControlTemplate}" Style="{DynamicResource PopupStyle}" Title="{DynamicResource l_popup.PopupSelectPnColor.Title}" Height="700" Width="1050">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="1*" />
      <ColumnDefinition Width="1*" />
      <ColumnDefinition Width="1*" />
      <ColumnDefinition Width="1*" />
      <ColumnDefinition Width="1*" />
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
      <RowDefinition Height="1*" />
      <RowDefinition Height="15*" />
    </Grid.RowDefinitions>
    <TextBlock Grid.Row="0" Style="{DynamicResource PnPopupHeaderText}" Text="{DynamicResource l_popup.PopupSelectPnColor.SolidLine}" Grid.ColumnSpan="3" Margin="1" />
    <src1:ListBoxPopup ScrollViewer.VerticalScrollBarVisibility="Hidden" ScrollViewer.HorizontalScrollBarVisibility="Hidden" Margin="1,1,207,1" BorderThickness="0" Background="#FFF5F5F5" Grid.Row="1" Grid.ColumnSpan="2" Grid.Column="0" ItemsSource="{Binding Colors1}" SelectedItem="{Binding Path=SelectedColor1, Mode=TwoWay}">
      <ItemsControl.ItemTemplate>
        <DataTemplate>
          <Border Name="border" BorderThickness="0" SnapsToDevicePixels="true" Height="25" Width="145">
            <Grid Name="contentPresenter">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="35" />
                <ColumnDefinition Width="1*" />
              </Grid.ColumnDefinitions>
              <Rectangle Stroke="#FF000000" Grid.Column="0" Width="70" Height="70" Margin="0" Fill="{Binding Brush}" />
              <Label Grid.Column="1" VerticalAlignment="Center">
                <TextBlock VerticalAlignment="Center">
                  <Run Text="{DynamicResource l_popup.PopupSelectPnColor.Color}" /> <Run Text=" " /> <Run Text="{Binding PnNumber, Mode=OneWay}" /></TextBlock>
              </Label>
            </Grid>
          </Border>
        </DataTemplate>
      </ItemsControl.ItemTemplate>
    </src1:ListBoxPopup>
    <src1:ListBoxPopup ScrollViewer.VerticalScrollBarVisibility="Hidden" ScrollViewer.HorizontalScrollBarVisibility="Hidden" Margin="1,1,208,1" BorderThickness="0" Background="#FFF5F5F5" Grid.Column="1" Grid.Row="1" Grid.ColumnSpan="2" ItemsSource="{Binding Colors2}" SelectedItem="{Binding Path=SelectedColor2, Mode=TwoWay}">
      <ItemsControl.ItemTemplate>
        <DataTemplate>
          <Border Name="border" BorderThickness="0" SnapsToDevicePixels="true" Height="25" Width="145">
            <Grid Name="contentPresenter">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="35" />
                <ColumnDefinition Width="1*" />
              </Grid.ColumnDefinitions>
              <Rectangle Stroke="#FF000000" Grid.Column="0" Width="70" Height="70" Margin="0" Fill="{Binding Brush}" />
              <Label Grid.Column="1" VerticalAlignment="Center">
                <TextBlock VerticalAlignment="Center">
                  <Run Text="{DynamicResource l_popup.PopupSelectPnColor.Color}" /> <Run Text=" " /> <Run Text="{Binding PnNumber, Mode=OneWay}" /></TextBlock>
              </Label>
            </Grid>
          </Border>
        </DataTemplate>
      </ItemsControl.ItemTemplate>
    </src1:ListBoxPopup>
    <src1:ListBoxPopup ScrollViewer.VerticalScrollBarVisibility="Hidden" ScrollViewer.HorizontalScrollBarVisibility="Hidden" Margin="1" BorderThickness="0" Background="#FFF5F5F5" Grid.Column="2" Grid.Row="1" ItemsSource="{Binding Colors3}" SelectedItem="{Binding Path=SelectedColor3, Mode=TwoWay}">
      <ItemsControl.ItemTemplate>
        <DataTemplate>
          <Border Name="border" BorderThickness="0" SnapsToDevicePixels="true" Height="25" Width="145">
            <Grid Name="contentPresenter">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="35" />
                <ColumnDefinition Width="1*" />
              </Grid.ColumnDefinitions>
              <Rectangle Stroke="#FF000000" Grid.Column="0" Width="70" Height="70" Margin="0" Fill="{Binding Brush}" />
              <Label Grid.Column="1" VerticalAlignment="Center">
                <TextBlock VerticalAlignment="Center">
                  <Run Text="{DynamicResource l_popup.PopupSelectPnColor.Color}" /> <Run Text=" " /> <Run Text="{Binding PnNumber, Mode=OneWay}" /></TextBlock>
              </Label>
            </Grid>
          </Border>
        </DataTemplate>
      </ItemsControl.ItemTemplate>
    </src1:ListBoxPopup>
    <TextBlock Grid.Row="0" Style="{DynamicResource PnPopupHeaderText}" Text="{DynamicResource l_popup.PopupSelectPnColor.DashedLine}" Margin="1" Grid.Column="3" />
    <src1:ListBoxPopup ScrollViewer.VerticalScrollBarVisibility="Hidden" ScrollViewer.HorizontalScrollBarVisibility="Hidden" Margin="1" BorderThickness="0" Background="#FFF5F5F5" Grid.Column="3" Grid.Row="1" ItemsSource="{Binding LineColors}" SelectedItem="{Binding Path=SelectedLineColor, Mode=TwoWay}">
      <ItemsControl.ItemTemplate>
        <DataTemplate>
          <Border Name="border" BorderThickness="0" SnapsToDevicePixels="true" Height="25" Width="145">
            <Grid Name="contentPresenter">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="35" />
                <ColumnDefinition Width="1*" />
              </Grid.ColumnDefinitions>
              <Rectangle Stroke="#FF000000" Grid.Column="0" Width="70" Height="70" Margin="0" Fill="{Binding Brush}" />
              <Label Grid.Column="1" VerticalAlignment="Center">
                <TextBlock VerticalAlignment="Center">
                  <Run Text="{DynamicResource l_popup.PopupSelectPnColor.Color}" /> <Run Text=" " /> <Run Text="{Binding PnNumber, Mode=OneWay}" /></TextBlock>
              </Label>
            </Grid>
          </Border>
        </DataTemplate>
      </ItemsControl.ItemTemplate>
    </src1:ListBoxPopup>
    <TextBlock Style="{DynamicResource PnPopupHeaderText}" Text="{DynamicResource l_popup.PopupSelectPnColor.ChainLine}" Margin="1" Grid.Column="4" />
    <src1:ListBoxPopup ScrollViewer.VerticalScrollBarVisibility="Hidden" ScrollViewer.HorizontalScrollBarVisibility="Hidden" Margin="1" BorderThickness="0" Background="#FFF5F5F5" Grid.Column="4" Grid.Row="1" ItemsSource="{Binding DotLineColors}" SelectedItem="{Binding Path=SelectedDotLineColor, Mode=TwoWay}">
      <ItemsControl.ItemTemplate>
        <DataTemplate>
          <Border Name="border" BorderThickness="0" SnapsToDevicePixels="true" Height="25" Width="145">
            <Grid Name="contentPresenter">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="35" />
                <ColumnDefinition Width="1*" />
              </Grid.ColumnDefinitions>
              <Rectangle Stroke="#FF000000" Grid.Column="0" Width="70" Height="70" Margin="0" Fill="{Binding Brush}" />
              <Label Grid.Column="1" VerticalAlignment="Center">
                <TextBlock VerticalAlignment="Center">
                  <Run Text="{DynamicResource l_popup.PopupSelectPnColor.Color}" /> <Run Text=" " /> <Run Text="{Binding PnNumber, Mode=OneWay}" /></TextBlock>
              </Label>
            </Grid>
          </Border>
        </DataTemplate>
      </ItemsControl.ItemTemplate>
    </src1:ListBoxPopup>
  </Grid>
</src1:PopupBase>